# ansible host
sudo apt install -y sshpass python3 python3-pip
pipx install --include-deps ansible
pip install ansible-pylibssh
pip install ncclient
ansible-galaxy install juniper.junos

mkdir keys
ssh-keygen -t rsa -b 2048 -N "" -C "" -f keys/srx
scp keys/srx.pub root@192.168.1.150:/tmp


# srx
config
delete security zones security-zone trust
delete security zones security-zone trust
delete interfaces 
delete vlans

delete security zones security-zone trust

set security zones security-zone oob tcp-rst
set security zones security-zone oob interfaces ge-0/0/0 unit 0 host-inbound-traffic system-services ping
set security zones security-zone oob interfaces ge-0/0/0 unit 0 host-inbound-traffic system-services netconf
set security zones security-zone oob interfaces ge-0/0/0 unit 0 host-inbound-traffic system-services ssh

set system login user 9admin full-name "927_admin" class super-user
set system login user 9admin authentication plain-text-password 

set system login user svc_ansible authentication load-key-file /tmp/srx.pub
set system login user svc_ansible class super-user
set system login user svc_ansible uid 3000



set system services netconf ssh